## 📁 Scripts Folder

This directory contains helper scripts that power the dashboard’s functionality, enabling automation, data processing, and visualization.

### 📄 Files Overview

- **`utils.py`**  
  Shared utility functions for:
  - Loading datasets
  - Preprocessing data
  - Generating visualizations

- **`main.py`**  
  The main entry point for the Streamlit dashboard application.

---

### ▶️ Running the Dashboard Locally

To launch the dashboard on your local machine, run the following command from the project root:

```bash
streamlit run app/main.py