# Scripts Folder

This folder contains helper scripts for automating or extending the dashboard features.

- `utils.py` — shared helper functions for loading and visualizing data.
- `main.py` — Streamlit app entry point.

To run the dashboard locally:
```bash
streamlit run app/main.py

